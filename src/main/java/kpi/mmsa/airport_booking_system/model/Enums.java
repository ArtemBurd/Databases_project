package kpi.mmsa.airport_booking_system.model;

public class Enums {
    public enum Role {God, Admin, Client};
    public enum Status {unbanned, banned};
    public enum Flight_status {Landed, Departed, Canseled, Delayed, Estimated, Scheduled}
    public enum Class {Economy, Business}
}
